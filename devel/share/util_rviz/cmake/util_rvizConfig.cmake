# File was automatically generated by mrt_cmake_modules
#
# This file exports the following variables:
# util_rviz_LIBRARIES: Library targets that should be linked against
# util_rviz_EXPORTED_TARGETS: As required by catkin
# util_rviz_EXPORTS_TARGETS: To indicate this package exports targets instead of plain libraries


####### Expanded from @PACKAGE_INIT@ by configure_package_config_file() #######
####### Any changes to this file will be overwritten by the next CMake run ####
####### The input file was packageConfig.cmake.in                            ########

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../" ABSOLUTE)

####################################################################################

set(util_rviz_LIBRARIES util_rviz::util_rviz;util_rviz::util_rviz_compiler_flags)  # things that dependend packages should link against

# catkin wants this. Don't bother with it.
set(util_rviz_EXPORTED_TARGETS)

# mark this as a catkin project
set(util_rviz_FOUND_CATKIN_PROJECT TRUE)
set(util_rviz_EXPORTS_TARGETS TRUE)

macro(_init_package_dependencies)
    # gets all targets that this package export_depends on and sets var to the name of these targets.
    # because this config file might be used recursively we have to be extra careful that variables set here do not affect each other
    if(TARGET util_rviz::auto_deps_export)
        return()
    endif()
    if(NOT EXISTS "${CMAKE_CURRENT_LIST_DIR}/auto_dep_vars.cmake")
        message(FATAL_ERROR "Project util_rviz did not export its dependencies properly! No auto_dep file was found.")
    endif()
    # Resolve deps in the same style they were resolved when this project was built
    include("${CMAKE_CURRENT_LIST_DIR}/auto_dep_vars.cmake") # sets _util_rviz_EXPORT_PACKAGES_

    set(util_rvizAutoDeps_PREFIX "util_rviz")
    set(util_rvizAutoDeps_NO_CATKIN_EXPORT TRUE) # disable variable export because catkin is not listening here
    if(NOT mrt_cmake_modules_FOUND)
        # We need the mrt_cmake_modules because they provide extra CMake Modules for finding thirdparty stuff
        find_package(mrt_cmake_modules REQUIRED)
    endif()
    find_package(util_rvizAutoDeps REQUIRED
        COMPONENTS ${_util_rviz_EXPORT_PACKAGES_}
        PATHS ${CMAKE_CURRENT_LIST_DIR} NO_DEFAULT_PATH
        )
    if(NOT OpenMP_FOUND)
        find_package(OpenMP) # openmp is by default always a dependency
    endif()
    unset(util_rvizAutoDeps_PREFIX)
    unset(util_rvizAutoDeps_NO_CATKIN_EXPORT)
endmacro()

if(util_rviz_LIBRARIES)
    _init_package_dependencies()
endif()

# add the targets
if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/util_rvizTargets.cmake")
    include("${CMAKE_CURRENT_LIST_DIR}/util_rvizTargets.cmake")
endif()

# add dependencies to other things generated by this package
set(util_rviz_targets )
if(util_rviz_targets AND NOT TARGET util_rviz::mrt_exported_targets)
    set(namespaced_exported_targets)
    foreach(t ${util_rviz_targets})
        if(NOT TARGET ${target})
            add_library(util_rviz::${t} INTERFACE IMPORTED)
            list(APPEND namespaced_exported_targets util_rviz::${t})
        else()
            list(APPEND namespaced_exported_targets ${t})
        endif()
    endforeach()
    add_library(util_rviz::mrt_exported_targets INTERFACE IMPORTED)
    add_dependencies(util_rviz::mrt_exported_targets ${namespaced_exported_targets})
    list(APPEND util_rviz_LIBRARIES util_rviz::mrt_exported_targets)
    unset(namespaced_exported_targets)
endif()

set(util_rviz_EXTRAS_FILE )
if(util_rviz_EXTRAS_FILE)
    include(${CMAKE_CURRENT_LIST_DIR}/${util_rviz_EXTRAS_FILE})
endif()
unset(util_rviz_EXTRAS_FILE)
unset(util_rviz_targets)
